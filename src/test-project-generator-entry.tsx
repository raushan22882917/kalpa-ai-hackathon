/**
 * Entry point for Project Generator Test
 */

import React from 'react';
import ReactDOM from 'react-dom/client';
import ProjectGeneratorTest from './components/ProjectGeneratorTest';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ProjectGeneratorTest />
  </React.StrictMode>
);
